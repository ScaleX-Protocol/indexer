{
  "name": "performance-tester",
  "version": "1.0.0",
  "description": "Performance testing tool for backend API comparison",
  "main": "src/index.ts",
  "scripts": {
    "test": "bun run src/index.ts",
    "dev": "bun --watch run src/index.ts",
    "build": "bun build src/index.ts --outdir=dist --target=node"
  },
  "dependencies": {
    "chalk": "^5.3.0",
    "cli-table3": "^0.6.5",
    "axios": "^1.6.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  }
}